<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tarea02AngelRamosDelgado"
        tools:targetApi="31">

        <!-- Configuración de la pantalla Splash -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Configuración de la actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Actividad para los detalles de un personaje -->
        <activity
            android:name=".ActividadDetallePersonaje"
            android:label="Detalles del Personaje" />

        <!-- Actividad para editar un personaje -->
        <activity
            android:name=".ActividadEditarPersonaje"
            android:label="Editar Personaje" />

    </application>

</manifest>
